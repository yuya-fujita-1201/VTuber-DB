# APIレスポンス形式の確認

## GET /api/tags

**レスポンス形式**: `{ "data": [...] }`

**問題**: テストスクリプトは配列を期待していたが、実際は `{ data: [...] }` の形式

## GET /api/stats

**予想レスポンス形式**: `{ "vtuber_count": 50, "tag_count": 60, ... }`

**問題**: テストスクリプトはトップレベルのプロパティを期待していたが、実際のレスポンス形式を確認する必要がある

## 修正が必要なテスト

1. GET /api/tags のレスポンス形式
2. GET /api/stats のレスポンス形式
3. タグslugのエンコーディング（日本語slug）
